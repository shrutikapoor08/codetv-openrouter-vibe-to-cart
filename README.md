## Instructions

1. Add a `.env.local` file w/ `TAVILY_API_KEY` and `OPENAI_API_KEY` defined. Copy `.env.example` (example file) if needed.
2. `npm i`
3. `npm run dev`
4. `node src/api/server.js` (in a separate terminal tab)
5. `open http://localhost:5173/`
